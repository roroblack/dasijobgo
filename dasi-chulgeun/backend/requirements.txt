# 다시,출근 프로토타입 백엔드 — FastAPI. 2026-07 기준 버전 핀.
# 결정론 로직(매칭·일정·역량갭)은 순수 파이썬, 외부 의존 없음.
# LLM 라이브 호출은 선택(anthropic). 키/SDK 없으면 템플릿 폴백(RULE §3.1).

fastapi==0.138.0
uvicorn[standard]==0.49.0
pydantic==2.13.4

# --- 테스트(선택) ---
pytest==8.3.4

# --- LLM 언어 계층(선택). 없으면 자동 폴백 ---
# anthropic==0.40.0
