# 백엔드 환경변수 예시. 실제 값은 .env 로 복사(커밋 금지).
# 미설정이어도 동작(폴백) — RULE §3.1.

# LLM 언어 계층(선택). 설정 시 온보딩·이력서·피드백 문장이 실제 생성으로 전환.
# ANTHROPIC_API_KEY=sk-ant-...
# LLM_MODEL=claude-sonnet-5

# 서버(선택)
# HOST=127.0.0.1
# PORT=8090

# 화상 링크 베이스(국산 API 연동 전까지 Meet 형식 폴백)
# MEET_BASE=https://meet.google.com
