# 다시,출근 백엔드 의존성 (2026-07 실측 버전 핀)
# 결정론 로직은 순수 파이썬(외부 의존 없음). STT/RAG/LLM 은 env-gate — 미설치 시 자동 폴백.

# ── 핵심(경량): 이것만으로 API·결정론 로직 동작 ──
fastapi==0.138.0
uvicorn[standard]==0.49.0
pydantic==2.13.4

# ── STT(음성인식): faster-whisper(CPU/int8). ctranslate2·av·onnxruntime·tokenizers 자동 설치 ──
faster-whisper==1.2.1

# ── RAG(임베딩): sentence-transformers. torch·transformers 자동 설치 ──
#   ※ CPU 전용 torch 권장:  pip install torch --index-url https://download.pytorch.org/whl/cpu
sentence-transformers==5.6.0

numpy==2.5.1

# ── 테스트 ──
pytest==8.3.4

# ── 선택(env-gate, 미설치 시 자동 폴백) ──
# anthropic==0.40.0        # 클라우드 LLM (ANTHROPIC_API_KEY 설정 시 활성)
# llama-cpp-python         # 로컬 GGUF LLM (LLM_GGUF_PATH 설정 시 활성, 예: Gemma-4-E4B)
